torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
tokenizers>=0.13.0
accelerate>=0.20.0
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0
tqdm>=4.65.0
sentencepiece>=0.1.99
safetensors>=0.3.0
einops>=0.6.1

# Optional (可选)
# deepspeed>=0.9.0  # 需要时手动安装
# flash-attn>=2.0.0  # 需要编译，可选
# wandb>=0.15.0  # 日志可选
